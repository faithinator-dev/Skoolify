// School routes
const express = require('express');
const router = express.Router();

// Placeholder for school routes
router.get('/', (req, res) => res.send('School routes'));

module.exports = router;
